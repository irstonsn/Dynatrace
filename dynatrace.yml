 tasks:
   - name: Run script using service account
     ansible.windows.win_shell: |
       powershell.exe -ExecutionPolicy Bypass -File C:\scripts\deploy.ps1 (chnage to batch if your are running batch script)
     become: yes
     become_method: runas
     become_user: EMEA\SRVEMA-MFGS-PMX
     vars:
       ansible_become_password: "PGSMfgsPmx@automation2026"
