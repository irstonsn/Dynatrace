---
- name: Run deployment script
  hosts: all
  tasks:
    - name: Run script using service account
      ansible.windows.win_shell: |
        powershell.exe -ExecutionPolicy Bypass -File C:\scripts\deploy.ps1
      become: true
      become_method: runas
      become_user: EMEA\SRVEMA-MFGS-PMX
